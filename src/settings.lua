-- Which side the computer will look for an inventory.
local INVENTORY_SIDE = "right"

-- Which side the computer will output a redstone signal to.
local REDSTONE_OUTPUT_SIDE = "left"

-- Which key should be pressed to exit the program
local STOP_KEY = keys.q

return {INVENTORY_SIDE = INVENTORY_SIDE, REDSTONE_OUTPUT_SIDE = REDSTONE_OUTPUT_SIDE, STOP_KEY = STOP_KEY}